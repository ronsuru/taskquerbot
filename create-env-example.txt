# Copy this content to create your .env file
# Replace the placeholder values with your actual Google Cloud information

# Google Cloud Storage Configuration
GOOGLE_APPLICATION_CREDENTIALS=./service-account-key.json
GOOGLE_CLOUD_PROJECT_ID=your-actual-project-id-here

# Database Configuration (replace with your database URL)
DATABASE_URL=postgresql://username:password@host:port/database

# TON Blockchain Configuration (replace with your actual values)
TON_API_KEY=your-tonapi-key
MNEMONIC_WALLET_KEY="your 24 word mnemonic phrase here"

# Server Configuration
PORT=5000
NODE_ENV=development

# Object Storage Configuration
PUBLIC_OBJECT_SEARCH_PATHS=/public,/uploads
